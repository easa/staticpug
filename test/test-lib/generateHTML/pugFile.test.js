const pugFile = require('../../../lib/generateHTML/pugFile');

test('Should return currect objects', () => {
  expect(typeof pugFile).toBe('function')
})

test('Should call -- once', () => { })
test('Should create files', () => { })