const templateFolder = require('../../../lib/generateHTML/templateFolder');

test('Should return currect objects', () => {
  expect(typeof templateFolder).toBe('function')
})

test('Should call -- once', () => { })
test('Should create files', () => { })